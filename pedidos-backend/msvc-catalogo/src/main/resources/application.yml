spring:
  application:
    name: msvc-catalogo

  datasource:
    url: jdbc:postgresql://localhost:5432/catalogo_db
    username: postgres
    password: Redimi2one2025*

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8082
